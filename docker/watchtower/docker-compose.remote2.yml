services:
  watchtower_remote2:
    container_name: watchtower_remote_${REMOTE2_NAME}
    image: nickfedor/watchtower
    volumes:
      - /etc/timezone:/etc/timezone:ro
    environment:
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_SCHEDULE=0 10 7 * * 6
      - WATCHTOWER_ROLLING_RESTART=true
      - DOCKER_HOST=${REMOTE2_URL}
    networks:
      net:
        ipv4_address: 198.19.8.4
    logging:
      driver: "json-file"
      options:
        max-size: "2m"
        max-file: "10"
    restart: always